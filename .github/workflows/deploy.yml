name: Deploy

on:
  push:
    branches: [main]
    
jobs:
  deploy:
    name: Deploy
    runs-on: self-hosted
    steps:
      - name: Run Deploy
        run: |
          cd ${{ secrets.REPO_PATH }}
          git pull origin main
          docker compose down
          docker compose up -d